1. To build the code: `./build_release.sh`
2. To run the code e2e: `./run_release.sh`

For debug and address sanitized versions:

1. To build the code: `./build.sh`
2. To run the code e2e: `./run.sh`
